import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class ApplicationForm {
  //array of pages

  message = 'hi';
  add() {
    //add to pages
  }

  submit() {
    //check pages if valid
  }
}
